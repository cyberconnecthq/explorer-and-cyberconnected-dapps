import { Network } from '@/utils/types';

export const NAME_SPACE = 'CyberConnect';
export const NETWORK = Network.ETH;
export const FIRST = 1000; // The number of users in followings/followers list for each fetch
